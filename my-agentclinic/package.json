{
  "name": "agentclinic",
  "version": "1.0.0",
  "private": true,
  "description": "Healthcare platform for AI agents",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "type-check": "pnpm -r type-check",
    "prisma": "prisma",
    "test": "pnpm -r test"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@prisma/client": "5.22.0"
  }
}
